This folder contains the SQLite database and initialization logic.
Run the backend once to create the database file automatically using the seed script in backend/init_db.py.
If the file app.db does not exist, it will be generated when the server starts.
